version: "3.9"

services:
  app:
    build: .
    volumes:
      - .:/app  # Monta tu código en el contenedor
    working_dir: /app
    command: python main.py
    environment:
      - PYTHONUNBUFFERED=1
